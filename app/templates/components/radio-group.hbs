{{!<fieldset>}}
    <div role="radiogroup">{{! menu?}}
{{#each items as |item|}}
        <label>
            <input type="radio" name={{name}} value={{item.value}} checked={{if (eq value item.value) 'checked'}} onchange={{action onchange}} />
            <span>{{item.label}}</span>
        </label>
{{/each}}
    </div>
{{!</fieldset>}}
