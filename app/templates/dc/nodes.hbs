    <h1>
        Nodes
    </h1>
{{#if true }}
    {{catalog-filter}}
    <h2>Unhealthy Nodes</h2>
    {{#filtered-list tagName="ul" items=items as |item|}}
        <li>
            {{ healthchecked-resource item=item href='dc.nodes.show'}}
        </li>
    {{/filtered-list}}
    <h2>Healthy Nodes</h2>
    {{#filtered-list tagName="ul" items=items as |item|}}
        <li>
            {{ healthchecked-resource item=item href='dc.nodes.show'}}
        </li>
    {{/filtered-list}}
{{else}}
    <p>There are no nodes.</p>
{{/if}}
    {{outlet}}