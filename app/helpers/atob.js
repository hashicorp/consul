import { helper } from '@ember/component/helper';
// const atob = window.atob;
// export atob window.atob;
// function atob([value = '']/*, hash*/) {
//   return atob(value);
// }

export default helper(window.atob);
