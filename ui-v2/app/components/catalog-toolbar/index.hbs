<form class="catalog-toolbar" data-test-catalog-toolbar>
  <FreetextFilter @onsearch={{action onsearch}} @value={{value}} @placeholder="Search" />
  <PopoverSelect
    data-popover-select
    @selected={{selected}}
    @options={{options}}
    @onchange={{onchange}}
    @title='Sort By'
  />
</form>
