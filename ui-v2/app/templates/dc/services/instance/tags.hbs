<div id="tags" class="tab-section">
  <div role="tabpanel">
    {{#if (gt item.Tags.length 0) }}
    <TagList @items={{item.Tags}} />
    {{else}}
      <p>
        There are no tags.
      </p>
    {{/if}}
  </div>
</div>
