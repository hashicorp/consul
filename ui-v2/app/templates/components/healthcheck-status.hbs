<dt title="{{capitalize name}}" class="{{name}}{{if (lt count 1) ' zero'}}">Healthchecks {{capitalize name}}</dt>
<dd title="{{capitalize name}}" class={{if (lt count 1) 'zero'}} style={{width}}>{{format-number count}}</dd>