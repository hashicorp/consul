import routingCreateQueryParams from 'consul-ui/utils/routing/create-query-params';
import { module, test } from 'qunit';

module('Unit | Utility | routing/create query params');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = routingCreateQueryParams();
  assert.ok(result);
});
