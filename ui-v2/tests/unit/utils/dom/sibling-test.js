import domSibling from 'consul-ui/utils/dom/sibling';
import { module, test } from 'qunit';

module('Unit | Utility | dom/sibling');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = domSibling();
  assert.ok(result);
});
