import domClosest from 'consul-ui/utils/dom/closest';
import { module, test } from 'qunit';

module('Unit | Utility | dom/closest');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = domClosest();
  assert.ok(result);
});
