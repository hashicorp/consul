import templatize from 'consul-ui/utils/templatize';
import { module, test } from 'qunit';

module('Unit | Utility | templatize');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = templatize();
  assert.ok(result);
});
