```release-note:feature
acl: Add OIDC RP-Initiated IdP logout so that `consul logout` and the UI logout can terminate the identity provider session for tokens created via an `oidc` auth method. Opt in with the `IDPLogout` auth method config option; it additionally requires the IdP to advertise an `end_session_endpoint` in its OIDC discovery document.
```
