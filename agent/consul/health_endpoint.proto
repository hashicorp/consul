syntax = "proto3";

package consul;

service Health {
    rpc Stream(TestRequest) returns (stream TestReply) {}
} 

message TestRequest {
    string name = 1;
}
  
message TestReply {
    int32 data = 1;
}