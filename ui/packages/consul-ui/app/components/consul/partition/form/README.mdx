# Consul::Partition::Form

```hbs
<DataLoader @src={{
  uri '/${partition}/${nspace}/${dc}/partition/${id}'
    (hash
      partition='partition'
      nspace='nspace'
      dc='dc'
      id=''
    )
  }}
as |loader|>
  <:loaded>
    <Consul::Partition::Form
      @item={{loader.data}}
      @dc={{'dc-1'}}
      @nspace={{'nspace'}}
      @partition={{'partition'}}
      @onsubmit={{noop}}
    />
  </:loaded>
</DataLoader>
```
