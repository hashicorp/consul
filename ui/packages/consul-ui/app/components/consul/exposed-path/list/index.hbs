{{!
  Copyright (c) HashiCorp, Inc.
  SPDX-License-Identifier: BUSL-1.1
}}

<div
  class="consul-exposed-path-list"
  ...attributes
>
  <ul
    data-test-proxy-exposed-paths
  >
  {{#each @items as |path|}}
    <li>
      <div class="header">
      {{#let (concat @address ':' path.ListenerPort path.Path) as |combinedAddress|}}
        <p class="combined-address">
          <span>
            {{combinedAddress}}
          </span>
          <ConsulCopyButton
            @value={{combinedAddress}}
            @name="Address"
          />
        </p>
      {{/let}}
      </div>
      <div class="detail">
      {{#if path.Protocol}}
        <dl class="protocol">
          <dt {{hds-tooltip "Protocol" options=(detached-tooltip-options)}}>
            Protocol
          </dt>
          <dd>
            {{path.Protocol}}
          </dd>
        </dl>
      {{/if}}
      {{#if path.ListenerPort}}
        <dl class="port">
          <dt {{hds-tooltip "Listener Port" options=(detached-tooltip-options)}}>
            Listener Port
          </dt>
          <dd>
            listening on :{{path.ListenerPort}}
          </dd>
        </dl>
      {{/if}}
      {{#if path.LocalPathPort}}
        <dl class="port">
          <dt {{hds-tooltip "Local Path Port" options=(detached-tooltip-options)}}>
            Local Path Port
          </dt>
          <dd>
            local port :{{path.LocalPathPort}}
          </dd>
        </dl>
      {{/if}}
      {{#if path.Path}}
        <dl class="path">
          <dt {{hds-tooltip "Path" options=(detached-tooltip-options)}}>
            Path
          </dt>
          <dd>
            {{path.Path}}
          </dd>
        </dl>
      {{/if}}
      </div>
    </li>
  {{/each}}
  </ul>
</div>
