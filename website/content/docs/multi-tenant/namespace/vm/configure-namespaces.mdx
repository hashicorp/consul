---
layout: docs
page_title: Configure Consul namespaces on VMs
description: |-
  Separate resource access and remove resource uniqueness restrictions using Consul Enterprise namespaces.
---

# Configure Consul namespaces on VMs

@include 'alerts/enterprise-alert.mdx'

Namespaces are the right solution to achieve complete data isolation between teams. In a fully isolated configuration, each team will have its own namespace. Each namespace will have an operator, referred to as *namespace operator*, responsible for managing data and access within their namespace. The namespace operator will only have access to view and update data and access in their namespace.

Namespaces are managed at datacenter level, this means that, to configure and manage namespaces you will need an ACL token who has permission over the entire datacenter. 

To create namespaces you need a token with [`operator = "write"`](/consul/docs/secure/acl/rule#operator-rules) policy. 

## Create namespace definitions

The first step to leverage namespaces in Consul is to create the namespaces to assign to the different teams that need to coexist in your datacenter.

In this document we'll assume the existence of two different teams, an *app-team* responsible for the deployment of the application, and a *db-team* responsible for the maintenance of the production DBMS.

Namespaces can be defined in configuration files, either in *hcl* or *json* format.

<Tabs>
<Tab heading="HCL">

Create the namespace definition file for the *app-team*.

<CodeBlockConfig filename="app-team.hcl">

```hcl
name = "app-team",
description = "Namespace for app-team managing the production application"
```

</CodeBlockConfig>

Create the namespace definition file for the *db-team*.

<CodeBlockConfig filename="db-team.hcl">

```hcl
name = "db-team",
description = "Namespace for db-team managing the production database"
```

</CodeBlockConfig>

</Tab>
<Tab heading="JSON">

Create the namespace definition file for the *app-team*.

<CodeBlockConfig filename="app-team.json">

```json
{
  "name": "app-team",
  "description": "Namespace for app-team managing the production application"
}
```

</CodeBlockConfig>

Create the namespace definition file for the *db-team*.

<CodeBlockConfig filename="db-team.json">

```json
{
  "name": "db-team",
  "description": "Namespace for db-team managing the production database"
}
```

</CodeBlockConfig>

</Tab>
</Tabs>

The examples use basic namespace definitions, with only the name
and description. To get a full list of available configuration parameters refer to the 
[namespace definition reference](/consul/docs/reference/namespace).

## Create namespaces

Create the namespaces using the [`consul namespace`](/consul/commands/namespace) CLI command, passing the created definitions as parameter.

<Tabs>
<Tab heading="HCL">

Create the `app-team` namespace.

```shell-session
$ consul namespace write app-team.hcl
Name: app-team
Description:
   Namespace for app-team managing the production application
Partition:   default
```

Create the `db-team` namespace.

```shell-session
$ consul namespace write db-team.hcl
Name: db-team
Description:
   Namespace for db-team managing the production counting application
Partition:   default
```

</Tab>
<Tab heading="JSON">

Create the `app-team` namespace.

```shell-session
$ consul namespace write app-team.json
Name: app-team
Description:
   Namespace for app-team managing the production application
Partition:   default
```

Create the `db-team` namespace.

```shell-session
$ consul namespace write db-team.json
Name: db-team
Description:
   Namespace for db-team managing the production counting application
Partition:   default
```

</Tab>
</Tabs>

## Review created namespaces

Use the [`consul namespace list`](/consul/commands/namespace/list) CLI command to review the namespaces.

```shell-session
$ consul namespace list
app-team:
   Description:
      Namespace for app-team managing the production application
   Partition:   default
db-team:
   Description:
      Namespace for db-team managing the production database
   Partition:   default
```

To get more information about one specific namespace you can use the [`consul namespace read <namespace>`](/consul/commands/namespace/list) CLI command.

```shell-session
$ consul namespace read app-team
Name: app-team
Description:
   Namespace for app-team managing the production application
Partition:   default
```

## Delete namespaces

After you create a namespace, you can update or delete it using the Consul [CLI](/consul/commands/namespace).



## Next steps

In this document, you learned how to create and interact with Consul Enterprise namespaces.

To learn how to to secure the resources within a namespace refer to [Delegate ACL token management with namespaces](/consul/docs/multi-tenant/namespace/vm/delegate-token-management).

To learn how to register services within a namespace refer to [Register services into a namespace](/consul/docs/register/service/namespace?utm_source=docs).
